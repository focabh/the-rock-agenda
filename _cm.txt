feat: popularidade Spotify + nudge de metadados + rebrand StageBoss

FASE 1 — Popularidade Spotify (desempate §3.2): refresh de token OAuth +
fetchTracksPopularity (GET /v1/tracks, lotes de 50). Coluna songs.popularidade,
botao "Popularidade" no repertorio, e desempate do arranjo por popularidade
(fallback "conhecida"). OBS: GET /tracks retorna 403 no app em dev-mode
(restricao Spotify nov/2024) — feature pronta e degradando p/ o proxy; quando o
app sair do dev-mode (Extended Quota) popula sozinho.

FASE 2 — Nudge de metadados: banner no repertorio quando ha musicas sem
energia/momento + selo (bolinha) por linha. Fecha o ciclo de import.

FASE 3 — Rebrand StageBoss + overhaul visual:
- "Backstage" -> "StageBoss" (layout, manifest, login, app-shell). Logo + "The
  Rock" ficam como conta ativa.
- Sistema de cores Zinc/Red/Amber via tokens (bg zinc-950, cards zinc-900,
  bordas zinc-800, titulos zinc-100, descricoes zinc-400, primario red-600).
- Amber EXCLUSIVO p/ cache (valor) e selo DROP (afinacao).
- Textura ultra-leve de palco (CSS puro: brilho vermelho + grelha de amp).
- PWA mobile: alvos de toque 44px so em telas de toque (pointer:coarse).

Sanity: tsc 0, build 0, suite pura 5/5 (palco~alvo, Final Boss, max 2/artista,
dropadas agrupadas, desempate por popularidade).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
